import { useEffect, useRef, useState } from 'react'
const stats=[{number:4,suffix:'+',label:'Major Projects'},{number:8,suffix:'+',label:'Core Technologies'},{text:'Full-Stack',label:'Development Focus'},{text:'2025',label:'BS Computer Science Graduate'}]
function Count({value,suffix}){const ref=useRef(null);const [count,setCount]=useState(0);useEffect(()=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const observer=new IntersectionObserver(([entry])=>{if(!entry.isIntersecting)return;if(reduced){setCount(value)}else{const start=performance.now();const tick=now=>{const progress=Math.min(1,(now-start)/900);setCount(Math.round(value*progress));if(progress<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)}observer.disconnect()},{threshold:.5});if(ref.current)observer.observe(ref.current);return()=>observer.disconnect()},[value]);return <strong ref={ref}>{count}{suffix}</strong>}
export default function Stats(){return <section className="stats" aria-label="Portfolio statistics">{stats.map(item=><div key={item.label} data-reveal>{item.number?<Count value={item.number} suffix={item.suffix}/>:<strong>{item.text}</strong>}<span>{item.label}</span></div>)}</section>}
