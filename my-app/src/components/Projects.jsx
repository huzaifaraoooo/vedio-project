import { projects } from '../data/projects'
export default function Projects(){return <section className="section projects" id="projects"><div className="section-label">04 / Selected work</div><div className="section-content"><p className="eyebrow" data-reveal>Four major builds</p><h2 data-reveal>Selected<br/><i>projects.</i></h2><div className="project-list">{projects.map((project,index)=><article className="project-row" key={project.id} data-reveal><span className="project-number">0{index+1}</span><div><h3>{project.title}</h3><p>{project.type} — {project.description}</p><div className="tag-list">{project.stack.map(tag=><span key={tag}>{tag}</span>)}</div></div><span className="project-arrow" aria-hidden="true">↗</span></article>)}</div></div></section>}
